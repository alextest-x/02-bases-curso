

//ponemos la interface porque no sabemos el tpo de dato que vamos a definir;

export interface Character{
  //character: Character;
  //ponemos id con ? para indicar que es opcional y elimina errores
  id?: string;
  name: string;
  power: number;

}
